spring:
  r2dbc:
    url: r2dbc:h2:mem:///test;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
  flyway:
    url: jdbc:h2:mem:test;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    locations: filesystem:src/main/resources/db/migration
    # Adicione esta linha para limpar o banco de dados na inicialização
    # clean-disabled: false # Garante que a limpeza não está desabilitada